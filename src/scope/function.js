function greeting() {
    let userName = 'Ana'; // Asignamos y declaramos la variable.
    console.log(useName); // Invocamos la variable

    if (userName === 'Ana') { // Se hace la validación 
        console.log(`Hello ${userName}!`) // Volvemos a invocarla
    }
}

greeting(); // Ejecución de la lógica 
console.log(userName);